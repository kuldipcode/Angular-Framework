name: Deploy to Amazon EC2

on:
  pull_request:
    branches: [main]
  push:
    branches: [dev]
jobs:
  build: 
    name: Build
    runs-on: ubuntu-latest
    defaults: 
    run:
      working-directory: ./
    
  deploy:
      name: Deploy
      runs-on: ubuntu-latest
      environment: production

